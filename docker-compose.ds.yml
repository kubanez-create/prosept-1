
services:
  ds:
    container_name: ds
    build: ./DS
    env_file: .env
    command: bash -c 'while !</dev/tcp/db/5432; do sleep 1; done; uvicorn src.main:app --reload --host 0.0.0.0 --port 8080'
    volumes:
      - ./DS:/app
    ports:
      - "8008:8080"